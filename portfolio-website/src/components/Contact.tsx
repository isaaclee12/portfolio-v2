import React from 'react';

const Contact: React.FC = () => {
  return (
    <section>
      {/* Contact component - contact info, form, social links */}
    </section>
  );
};

export default Contact;